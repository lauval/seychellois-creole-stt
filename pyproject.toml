[project]
name = "speech-to-text"
version = "0.1.0"
description = "ASR evaluation pipeline for Seychellois Creole (Kreol Seselwa)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.0.0",
    "datasets>=3.0.0",
    "ffmpeg-python>=0.2.0",
    "torch>=2.12.1",
    "torchaudio>=2.11.0",
    "torchcodec>=0.15.0",
    "transformers>=4.30.0",
    "whisperx>=3.7.2",
    "yt-dlp>=2024.0.0",
]
