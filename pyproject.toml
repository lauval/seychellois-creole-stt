[project]
name = "speech-to-text"
version = "0.1.0"
description = "ASR evaluation pipeline for Seychellois Creole (Kreol Seselwa)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ffmpeg-python>=0.2.0",
    "torch>=2.12.1",
    "whisperx>=3.7.2",
    "yt-dlp>=2024.0.0",
]
